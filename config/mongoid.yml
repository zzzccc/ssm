development:
  sessions:
    default:
      database: ssm_development
      hosts:
        - localhost:27017
      options:
  options:
test:
  sessions:
    default:
      database: ssm_test
      hosts:
        - localhost:27017
      options:
        read: primary
        max_retries: 1
        retry_interval: 0
